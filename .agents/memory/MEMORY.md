- [Orval codegen gotchas](orval-codegen-gotchas.md) — path-param/query-name collisions break codegen; api-server dev server needs restart to pick up route changes before smoke tests.
- [Expo native version mismatch](expo-native-version-mismatch.md) — Android NoSuchMethodError = expo-* pkg pinned to wrong-SDK version; fix via `expo install --check` + pnpm prune.
- [Native builds & native-only features](native-builds.md) — NFC/contacts/notifications etc. must be tested via dev-client/EAS build, not Expo Go; guard native imports so web/Expo Go degrade gracefully.
- [NFC/QR shared parser](nfc-qr-shared-parser.md) — parseQr (lib/contact-parse.ts) handles vCard + MECARD + freetext; use FN cross-reference to detect N-field order (many generators emit Given;Family not Family;Given); FN cross-reference trumps blind spec trust.
- [QR ML Kit raw vs data](qr-mlkit-raw-vs-data.md) — Android QR scans detect but extract nothing because ML Kit's result.data is a lossy parsed value; full payload is in result.raw. Always use parseQrBest(raw,data).
- [Mobile Android icon tofu](mobile-android-icons.md) — Feather icons box-out on Android/Expo Go because icon FONTS are unreliable there (family collision + no re-layout on late load); real fix = render icons as SVG (react-native-feather/react-native-svg), not a font.
- [Legacy role normalization](legacy-role-normalization.md) — stale DB role (company_admin) after a code rename 403s every gated write (whole capture engine); normalize at the auth boundary so it self-heals. 403≠OCR bug.
- [Mobile global query invalidation](mobile-query-invalidation.md) — Orval hooks don't invalidate; a global MutationCache.onSuccess in app/_layout invalidates all queries so dashboard/lists stay fresh without restart. Don't add per-call invalidation.
- [Drizzle PATCH empty-set 500](api-patch-empty-set.md) — every PATCH route must guard empty updateData (400, not 500) before db.update().set({}) and destructure req.body ?? {}; bad/empty input is a client error.
- [Mobile i18n + RTL + OCR language](mobile-i18n.md) — EN/AR locale parity is load-bearing; RTL is JS-driven (no reload); OCR `original` never stores a translation; offline scans translate by capture-time language.
- [Mobile batch-store OCR lifecycle](mobile-batch-ocr.md) — setBatchCaptures must NOT clear ocrResults (background OCR fires setBatchOcrResult before setBatchCaptures); clear only in clearBatchCaptures (end of session). batch-review must poll for "pending" status before falling back to sequential OCR.
- [Mobile scroll flexGrow](mobile-scroll-flexgrow.md) — all vertical ScrollView/FlatList/KeyboardAwareScrollView need flexGrow:1 in contentContainerStyle or drag-over-empty-space does nothing; also add keyboardShouldPersistTaps="handled". Remove scrollEnabled={list.length>0} gates (they break pull-to-refresh on empty state).
- [Mobile scan & mutation performance](mobile-performance.md) — server was never the "30s"; never await Gemini on a save response; deferred DB scores don't invalidate RQ; set camera pictureSize (don't full-sensor+shrink); thinkingBudget:0; optimistic onMutate.
- [Helmet on the cross-origin API](helmet-cross-origin-api.md) — must disable CORP/COEP/CSP or browsers refuse cross-origin scan images; passes tests/smoke but breaks only in a real browser.
- [Cross-currency pipeline totals](cross-currency-pipeline-totals.md) — convert each lead to display currency BEFORE summing; bug hides on every total surface; client-side derivation needs refetch-on-pull + has a list-limit truncation trap.
- [Mobile PIN fallback pattern](mobile-pin-fallback.md) — PinPad uses resetSignal (increment) not error bool; SecureStore PIN stored plaintext (TEE-encrypted, equivalent to hash); refreshPinAvailability() must be called after save/clear so overlay reacts without restart.
- [Soft-delete parity traps](soft-delete-parity-traps.md) — hard→soft conversion: softDelete must hand-replicate the onDelete cascade in a txn; UPDATE helpers called after a precheck (e.g. async scorer) need an isNull(deletedAt) guard or they write to/notify dead rows.
- [MFA company-required policy hook](mfa-required-policy-hook.md) — computing mfaNeeded but only challenging when user.mfaEnabled bypasses company-required MFA for unenrolled users; split the gate, issue no operational token, record no failed attempt.
- [OpenAPI optional-field ripple](openapi-optional-field-ripple.md) — loosening a shared response field (e.g. AuthResponse.token optional) breaks EVERY codegen consumer incl. mobile; run full `pnpm run typecheck` and guard each call site.
- [Refresh-token family revocation](refresh-rotation-revoke.md) — revoke a refresh family ONLY on proven replay (presented secret matches prior hash) + look up by unguessable familyId, else `<guessedId>.<garbage>` is an unauthenticated forced-logout DoS.
- [Login rate limiter counts failures only](login-ratelimiter-skip-success.md) — per-IP credential guard must skipSuccessfulRequests; counting successful logins false-positives shared-IP/NAT and breaks live integration suites (cumulative localhost logins → 429).
- [RBAC grant subset guard](rbac-grant-subset-guard.md) — assigning custom roles needs a permission-subset check vs the caller's own perms, not just base-role rank; else self-escalation via custom roles.
- [Invitation roleIds escalation](invitation-roleids-escalation.md) — assigning custom roleIds needs BOTH target-company scoping (firstRoleNotInCompany) AND a permission-subset check (mirror users.service#setUserRoles); accessibility alone is a back door.
- [Platform-owner tenant firewall](platform-owner-tenant-firewall.md) — platform_owner fails OPEN on tenantScope+requirePermission; block customer CRM with a path-scoped terminating guard placed before those gates.
- [Background jobs & queue](background-jobs-queue.md) — in-process queue for async email/maintenance; worker MUST skip (not throw) when email unconfigured but throw on transport error (else dead-letter flood / no retry); audit retention opt-in.
- [Caller-scoped vs tenant-scoped FK checks](fk-caller-vs-tenant-scope.md) — refAccessible(caller,...) lets a platform_owner/multi-company caller bind tenant A's row to tenant B's FK; binding a record to ANOTHER record's tenant needs refInCompany(table, cid, id) (target-company-scoped), not caller-scoped.
- [AI insight grounding & tenant-scoped FK derefs](ai-insight-grounding-tenant-scope.md) — insights derive from real rows only; read-path FK name lookups (event/contact/org) must be companyId-scoped, not id-alone — read counterpart to the write-path guard.
- [Reports permission policy & gated-read lockout](reports-permission-policy.md) — adding requirePermission to a previously-open read silently locks out admin/employee lacking the perm; audit live data + backfill jsonb perms, not just seed. Reports: platform_owner blocked, primary_admin bypass, admin default-on, employee opt-in.
- [Observability & monitoring](observability-monitoring.md) — readyz storage degrades (200) not gates (503); metrics per-process; login_attempts tenant-scoped via user_id; audit/alerts filters narrow-only.
- [Analytics micro-cache + write-epoch](analytics-microcache.md) — expensive report GETs cached ~30s; invalidation is one global writeEpoch bumped on any successful non-GET; key includes userId to stop cross-user leak. Don't add per-tenant invalidation.
- [Analytics top-performer per-user metrics](analytics-top-performers.md) — each per-user column must come from its OWN entity query; reusing a sibling count (contacts as "leads") typechecks + passes shallow tests but silently mislabels/mis-ranks.
- [Document version-number concurrency](document-version-concurrency.md) — max+1 append races; a bare retry-on-unique loop re-collides (no backoff). Serialize per-parent with pg_advisory_xact_lock + keep the unique index as backstop.
- [Metro crawl vs server-only pnpm dirs](metro-pdfkit-blocklist.md) — Expo Metro ENOENT-crashes crawling the workspace .pnpm store on stale install staging dirs of server-only deps (pdfkit); blockList them in metro.config.js; log tail can end on a stale crash while process is healthy.
- [Import/export access-control gates](import-export-gates.md) — whole import flow (preview/validate/commit) must share the create-permission gate, not just commit (validate leaks dup existence); export CSV/XLSX cells need formula-injection neutralization.
- [Polymorphic custom-field values, no cascade](polymorphic-custom-fields-no-cascade.md) — no FK cascade; any hard-delete/merge of the owner must delete its values in the same txn or they orphan silently.
- [Manual lead-assign clears teamId](lead-assign-teamid-clearing.md) — manual assign treats explicit teamId (incl null) as a SET; owner-only reassign clients must OMIT teamId, not send null, or team bindings wipe silently.
- [Round-robin persistent cursor](round-robin-persistent-cursor.md) — true round-robin needs a stored monotonic per-pool counter under advisory lock; count(assigned)%n repeats on reassign and rewinds on delete (lead_history cascades too).
- [Api-server limiter restart for gate](api-server-limiter-restart.md) — reset the login limiter by restarting the `artifacts/api-server: API Server` workflow (NOT `Project`), then run the suite ONCE; an aborted `pnpm test` still counts as a run.
- [Mentions + saved-search contract](mention-and-saved-search-contract.md) — notes store `@[Name](id)` tokens (server derives mentions authoritatively, no dangerouslySetInnerHTML); saved searches/filters/views share one table via entityType+kind with opaque JSON payload; apply merges over defaults.
- [Capture intelligence engine](capture-intelligence-engine.md) — read-only analyze warns/recognizes but never writes; provenance stays honest; scan meta cols are JSON text (parse on EVERY response path incl. getScan).
- [Unknown payloads + export filter subset](unknown-api-payload-and-export-filter-subset.md) — normalize `unknown` saved-view payloads field-by-field before applying (never blind-cast); export dialog must forward only server-supported filter keys (leads: single stage/assignedToId/eventId), not `{}` nor the rich set.
- [AI copilot deterministic-core + soft-degrade](ai-copilot-deterministic-softdegrade.md) — followup/coaching always ground a deterministic core then best-effort AI phrase; LLM-only types soft-degrade 200 not 500; deterministic rows never carry AI provenance; applicability enforced on single AND batch; never auto-send/write CRM.
- [AI insight provenance & CRM-only grounding](ai-insight-provenance-and-grounding.md) — stamp the REAL runtime provider/model (resolveSettings) not hardcoded; Stage 5A prompts must be strictly CRM-grounded (no "general knowledge" escape hatch); bump prompt version on material text change.
- [Settings runtime source of truth](settings-runtime-source-of-truth.md) — a per-tenant override that's PATCHable + shown by GET but ignored by the execution path is a silent reports-X-does-Y bug; make reads AND the runtime call the SAME resolver.
- [Apply-handoff field mapping is per-entity](apply-handoff-field-schema.md) — AI "Apply" routes a recommendation through manual PATCH; the mapped field must exist on THAT entity (leads have assignedToId but NOT followUpDate; contacts have both) or PATCH strips it → empty update → 400.
- [Background job AuthUser rebuild](background-job-authuser.md) — async workers must rebuild the full AuthUser via loadAuthUserById; a partial {id,companyId,role} breaks tenant-scoped reads (accessibleCompanies.length).
